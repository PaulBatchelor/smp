#!/bin/sh
if [ "$#" -lt 2 ]
then
echo "Usage: $0 UUID TAG"
fi


UUID=$(weewiki zet resolve $1)

if [[ ! $? -eq 0 ]]
then
    exit 1
fi

shift 1
TAGS=$*

for t in $TAGS
do
    # TODO: check to see if it has already been tagged
    weewiki zet link $UUID @$t
done
